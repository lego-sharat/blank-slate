# Supabase Edge Functions - Local Development Secrets
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# Supabase Project Reference (REQUIRED for deployment)
# Get from: Supabase Dashboard → Settings → General → Reference ID
SUPABASE_PROJECT_REF=your-project-ref-here

# Google OAuth Credentials
# Get from: https://console.cloud.google.com/apis/credentials
# You can use either naming convention (script accepts both):
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-your-client-secret
# OR:
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Anthropic API Key (for Claude Haiku)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# OAuth Token Encryption Key (OPTIONAL for local testing)
# Generate with: openssl rand -base64 32
# If not provided, test-local.sh will generate a random one each time
# Set this if you want a consistent encryption key across local restarts
# ENCRYPTION_KEY=your-base64-encryption-key-here

# Production Supabase Settings (REQUIRED for production deployment)
# Get from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Local Development (automatically provided by local CLI)
# These are set automatically when you run: supabase start
# SUPABASE_URL=http://localhost:54321
# SUPABASE_ANON_KEY=...
